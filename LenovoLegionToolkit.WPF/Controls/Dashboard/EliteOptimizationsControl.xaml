<UserControl
    x:Class="LenovoLegionToolkit.WPF.Controls.Dashboard.EliteOptimizationsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:custom="clr-namespace:LenovoLegionToolkit.WPF.Controls.Custom"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml">

    <custom:CardControl Margin="0,0,0,16" Icon="BrainCircuit24">
        <custom:CardControl.Header>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock FontWeight="SemiBold" FontSize="16" Text="AI / ML Performance System" />
                    <TextBlock
                        Margin="0,2,0,0"
                        FontSize="11"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="Intelligent optimization with real-time monitoring"
                        TextWrapping="Wrap" />
                </StackPanel>

                <!-- Real-time Metrics (Compact) -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="16,0,0,0">
                    <Border Padding="8,4" Margin="0,0,8,0" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4">
                        <StackPanel>
                            <TextBlock FontSize="9" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="CPU" HorizontalAlignment="Center" />
                            <TextBlock x:Name="_cpuTemp" FontSize="14" FontWeight="Bold" Text="--°C" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <Border Padding="8,4" Margin="0,0,8,0" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4">
                        <StackPanel>
                            <TextBlock FontSize="9" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="CPU FAN" HorizontalAlignment="Center" />
                            <TextBlock x:Name="_cpuFanSpeed" FontSize="14" FontWeight="Bold" Text="--" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <Border Padding="8,4" Margin="0,0,8,0" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4">
                        <StackPanel>
                            <TextBlock FontSize="9" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="GPU FAN" HorizontalAlignment="Center" />
                            <TextBlock x:Name="_gpuFanSpeed" FontSize="14" FontWeight="Bold" Text="--" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <Border Padding="8,4" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4">
                        <StackPanel>
                            <TextBlock FontSize="9" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="PERF" HorizontalAlignment="Center" />
                            <TextBlock x:Name="_perfImprovement" FontSize="14" FontWeight="Bold" Foreground="#10B981" Text="↑94%" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </custom:CardControl.Header>

        <StackPanel Margin="0,4,0,4">

            <!-- Thermal Management (Compact) -->
            <Expander Margin="0,0,0,8" IsExpanded="True">
                <Expander.Header>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <wpfui:SymbolIcon
                            Grid.Column="0"
                            Margin="0,0,8,0"
                            FontSize="16"
                            Foreground="#6366F1"
                            Symbol="WeatherHailNight24" />

                        <TextBlock Grid.Column="1" VerticalAlignment="Center" FontWeight="SemiBold" Text="Thermal Management" />

                        <wpfui:ToggleSwitch
                            x:Name="_aiAdaptiveFanToggle"
                            Grid.Column="2"
                            Margin="8,0,0,0"
                            Checked="AiAdaptiveFanToggle_Checked"
                            Unchecked="AiAdaptiveFanToggle_Unchecked" />
                    </Grid>
                </Expander.Header>

                <StackPanel Margin="24,8,0,0">
                    <!-- Manual Fan Controls (Compact) -->
                    <StackPanel x:Name="_manualFanControls">
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="50" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="11" Text="CPU Fan" />
                            <Slider x:Name="_cpuFanSlider" Grid.Column="1" Margin="8,0" VerticalAlignment="Center" Minimum="0" Maximum="100" Value="50" TickFrequency="10" IsSnapToTickEnabled="True" ValueChanged="CpuFanSlider_ValueChanged" />
                            <TextBlock x:Name="_cpuFanSliderValue" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" FontWeight="SemiBold" FontSize="11" Text="50%" />
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="50" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="11" Text="GPU Fan" />
                            <Slider x:Name="_gpuFanSlider" Grid.Column="1" Margin="8,0" VerticalAlignment="Center" Minimum="0" Maximum="100" Value="50" TickFrequency="10" IsSnapToTickEnabled="True" ValueChanged="GpuFanSlider_ValueChanged" />
                            <TextBlock x:Name="_gpuFanSliderValue" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" FontWeight="SemiBold" FontSize="11" Text="50%" />
                        </Grid>
                    </StackPanel>

                    <!-- Adaptive Fan Info (Compact) -->
                    <custom:InfoBar
                        x:Name="_adaptiveFanInfoBar"
                        Margin="0,4,0,0"
                        IsClosable="False"
                        IsOpen="False"
                        Severity="Success" />
                </StackPanel>
            </Expander>

            <!-- All Features (Collapsible) -->
            <Expander Margin="0,0,0,8" IsExpanded="False">
                <Expander.Header>
                    <StackPanel Orientation="Horizontal">
                        <wpfui:SymbolIcon Margin="0,0,8,0" FontSize="16" Foreground="#10B981" Symbol="CheckmarkCircle24" />
                        <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="Performance Features" />
                        <TextBlock VerticalAlignment="Center" Margin="8,0,0,0" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                            <Run Text="(" />
                            <Run x:Name="_activeFeatureCountText" Text="5/9" />
                            <Run Text=" active)" />
                        </TextBlock>
                    </StackPanel>
                </Expander.Header>

                <StackPanel Margin="24,8,0,0">
                    <!-- Core Performance (Compact Grid) -->
                    <TextBlock Margin="0,0,0,4" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Core Performance" />
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Row 0 -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,8,4">
                            <wpfui:SymbolIcon FontSize="14" Foreground="#10B981" Symbol="Database24" Margin="0,0,6,0" />
                            <TextBlock VerticalAlignment="Center" FontSize="11" Text="WMI Caching" />
                        </StackPanel>
                        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,4">
                            <wpfui:SymbolIcon FontSize="14" Foreground="#10B981" Symbol="Shield24" Margin="0,0,6,0" />
                            <TextBlock VerticalAlignment="Center" FontSize="11" Text="Memory Fixes" />
                        </StackPanel>

                        <!-- Row 1 -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="0,0,8,4">
                            <wpfui:SymbolIcon FontSize="14" Foreground="#10B981" Symbol="ArrowSync24" Margin="0,0,6,0" />
                            <TextBlock VerticalAlignment="Center" FontSize="11" Text="Async Prevention" />
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,4">
                            <wpfui:SymbolIcon FontSize="14" Foreground="#10B981" Symbol="DesktopFlow24" Margin="0,0,6,0" />
                            <TextBlock VerticalAlignment="Center" FontSize="11" Text="Non-blocking UI" />
                        </StackPanel>

                        <!-- Row 2 -->
                        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal" Margin="0,0,8,0">
                            <wpfui:SymbolIcon FontSize="14" Foreground="#10B981" Symbol="Color24" Margin="0,0,6,0" />
                            <TextBlock VerticalAlignment="Center" FontSize="11" Text="Parallel RGB" />
                        </StackPanel>
                    </Grid>

                    <!-- AI/ML Intelligence (Compact) -->
                    <TextBlock Margin="0,0,0,4" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="AI/ML Intelligence" />
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <wpfui:SymbolIcon x:Name="_mlAiIcon" Grid.Row="0" Grid.Column="0" Margin="0,0,6,4" FontSize="14" Symbol="LightbulbFilament24" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Margin="0,0,8,4" VerticalAlignment="Center" FontSize="11" Text="ML Power Predictor" />
                        <TextBlock x:Name="_mlAiStatus" Grid.Row="0" Grid.Column="2" Margin="0,0,0,4" VerticalAlignment="Center" FontSize="10" Text="Disabled" />

                        <wpfui:SymbolIcon x:Name="_adaptiveFanIcon" Grid.Row="1" Grid.Column="0" Margin="0,0,6,0" FontSize="14" Symbol="WeatherHailNight24" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,0,8,0" VerticalAlignment="Center" FontSize="11" Text="Adaptive Fan Curves" />
                        <TextBlock x:Name="_adaptiveFanStatus" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" FontSize="10" Text="Disabled" />
                    </Grid>

                    <!-- Resource Management (Compact) -->
                    <TextBlock Margin="0,0,0,4" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Resource Management" />
                    <Grid Margin="0,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <wpfui:SymbolIcon x:Name="_objectPoolingIcon" Grid.Row="0" Grid.Column="0" Margin="0,0,6,4" FontSize="14" Symbol="Storage24" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Margin="0,0,8,4" VerticalAlignment="Center" FontSize="11" Text="Object Pooling" />
                        <TextBlock x:Name="_objectPoolingStatus" Grid.Row="0" Grid.Column="2" Margin="0,0,0,4" VerticalAlignment="Center" FontSize="10" Text="Disabled" />

                        <wpfui:SymbolIcon x:Name="_reactiveSensorsIcon" Grid.Row="1" Grid.Column="0" Margin="0,0,6,0" FontSize="14" Symbol="Flash24" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,0,8,0" VerticalAlignment="Center" FontSize="11" Text="Reactive Sensors" />
                        <TextBlock x:Name="_reactiveSensorsStatus" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" FontSize="10" Text="Disabled" />
                    </Grid>
                </StackPanel>
            </Expander>

            <!-- AI/ML Suggestion (Compact) -->
            <custom:InfoBar
                x:Name="_aiSuggestionInfoBar"
                Margin="0,0,0,0"
                IsClosable="False"
                IsOpen="False"
                Severity="Informational" />

        </StackPanel>
    </custom:CardControl>

</UserControl>
